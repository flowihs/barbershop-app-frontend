import { HeaderBarberSide } from '../../widgets/profile/barber';

function ProfilePage() {
  return (
    <div className="bg-bg-primary">
      <HeaderBarberSide provisionId={1} />
    </div>
  );
}

export default ProfilePage;
